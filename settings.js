(function () {
  const DEFAULT_SETTINGS = {
    enabled: true,
    keepRecentTurns: 24,
    revealBatchSize: 8,
    useVisibilityBoost: true,
    compactActionBars: true
  };

  window.OGC_SHARED = {
    DEFAULT_SETTINGS
  };
})();
